 <!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <title>Sistem Informasi Direktori Tugas Akhir</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-slate-100 min-h-screen text-gray-800">

<!-- ===== HEADER ===== -->
<header class="bg-blue-900 text-white">
  <div class="max-w-7xl mx-auto px-6 py-6">
    <h1 class="text-2xl font-bold tracking-wide">
      Aplikasi Direktori Tugas Akhir Mahasiswa Mata Kuliah Teknologi Server
    </h1>
    <p class="text-sm opacity-90 mt-1">
      Fakultas Teknologi Informasi — Praktikum Teknologi Server (Tekser D)
    </p>
  </div>
</header>

<!-- ===== CONTENT ===== -->
<main class="max-w-7xl mx-auto px-6 py-10 space-y-10">

<!-- ===== INFO BAR ===== -->
<div class="bg-white border-l-4 border-blue-700 p-4 rounded shadow-sm">
  <p class="text-sm text-gray-700">
    Halaman ini digunakan untuk mengelola data judul tugas akhir mahasiswa
    yang tersimpan pada sistem.
  </p>
</div>

<!-- ===== FORM INPUT ===== -->
<section class="bg-white rounded-lg shadow">
  <div class="border-b px-6 py-4">
    <h2 class="text-lg font-semibold text-gray-700">
      📄 Form Input Data Tugas Akhir
    </h2>
    <p class="text-sm text-gray-500">
      Isikan data mahasiswa dan judul tugas akhir secara lengkap
    </p>
  </div>

  <form method="POST" action="/add" class="px-6 py-6 grid grid-cols-1 md:grid-cols-2 gap-5">
    <div>
      <label class="block text-sm font-medium mb-1">Nama Mahasiswa</label>
      <input name="nama" required
        class="w-full border px-3 py-2 rounded focus:ring focus:border-blue-500" />
    </div>

    <div>
      <label class="block text-sm font-medium mb-1">NIM</label>
      <input name="nim" required
        class="w-full border px-3 py-2 rounded focus:ring focus:border-blue-500" />
    </div>

    <div>
      <label class="block text-sm font-medium mb-1">Program Studi</label>
      <input name="prodi" required
        class="w-full border px-3 py-2 rounded focus:ring focus:border-blue-500" />
    </div>

    <div class="md:col-span-2">
      <label class="block text-sm font-medium mb-1">
        Judul / Deskripsi Tugas Akhir
      </label>
      <textarea name="content" rows="3" required
        class="w-full border px-3 py-2 rounded focus:ring focus:border-blue-500"></textarea>
    </div>

    <div class="md:col-span-2 text-right">
      <button
        class="bg-blue-700 hover:bg-blue-800 text-white px-6 py-2 rounded">
        💾 Simpan Data
      </button>
    </div>
  </form>
</section>

<!-- ===== SEARCH ===== -->
<section class="bg-white rounded-lg shadow px-6 py-4">
  <form method="GET" action="/" class="flex gap-3">
    <input
      type="text"
      name="search"
      value="<%= search || '' %>"
      placeholder="🔍 Cari judul tugas akhir..."
      class="flex-1 border px-3 py-2 rounded focus:ring focus:border-blue-500"
    />
    <button class="bg-gray-800 hover:bg-gray-900 text-white px-5 rounded">
      Cari
    </button>
  </form>
</section>

<!-- ===== TABLE ===== -->
<section class="bg-white rounded-lg shadow">
  <div class="border-b px-6 py-4 flex justify-between items-center">
    <div>
      <h2 class="text-lg font-semibold text-gray-700">
        📊 Daftar Tugas Akhir Mahasiswa
      </h2>
      <p class="text-sm text-gray-500">
        Data tersimpan dalam basis data sistem
      </p>
    </div>
    <span class="text-sm bg-blue-100 text-blue-800 px-3 py-1 rounded-full">
      Total Data: <%= menfess.length %>
    </span>
  </div>

  <div class="overflow-x-auto px-6 py-4">
    <table class="w-full text-sm border border-gray-200">
      <thead class="bg-gray-100 text-gray-700">
        <tr>
          <th class="border px-3 py-2 text-center">No</th>
          <th class="border px-3 py-2">Nama</th>
          <th class="border px-3 py-2">NIM</th>
          <th class="border px-3 py-2">Prodi</th>
          <th class="border px-3 py-2">Judul Tugas Akhir</th>
          <th class="border px-3 py-2 text-center">Aksi</th>
        </tr>
      </thead>

      <tbody>
        <% if (menfess.length === 0) { %>
          <tr>
            <td colspan="6" class="text-center py-8 text-gray-500">
              Belum ada data tugas akhir yang tersimpan
            </td>
          </tr>
        <% } %>

        <% menfess.forEach((m, i) => { %>
          <tr class="hover:bg-slate-50">
            <td class="border px-3 py-2 text-center"><%= i + 1 %></td>
            <td class="border px-3 py-2"><%= m.nama %></td>
            <td class="border px-3 py-2"><%= m.nim %></td>
            <td class="border px-3 py-2"><%= m.prodi %></td>
            <td class="border px-3 py-2"><%= m.content %></td>
            <td class="border px-3 py-2 text-center">
              <form method="POST" action="/delete/<%= m.id %>">
                <button class="text-red-600 hover:underline"
                        onclick="return confirm('Yakin ingin menghapus data ini?')">
                  Hapus
                </button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</section>

</main>

<!-- ===== FOOTER ===== -->
<footer class="bg-white border-t mt-14">
  <div class="max-w-7xl mx-auto px-6 py-4 text-sm text-gray-500 text-center">
    © 2026 Sistem Informasi Direktori Tugas Akhir Mahasiswa  
    <br />
    Praktikum Teknologi Server — Kelas Tekser D
  </div>
</footer>

</body>
</html>

## END OF FILE ##